server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://mysql.railway.internal:3306/cinehub
    username: root
    password: TaSjrkvKlXGuNxpTzyCMlLZrjNkpjEuP
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

# Logging configuration (optional)
logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Application-specific config (optional)
cinehub:
  api:
    version: v1
