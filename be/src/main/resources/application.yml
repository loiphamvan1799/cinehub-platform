server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://centerbeam.proxy.rlwy.net:15349/railway?useSSL=true&serverTimezone=UTC
    username: root
    password: AEvjnAaRQJcWoAeoHBCvhxmPYkTaQQVW
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      max-lifetime: 300000
      idle-timeout: 60000
      connection-timeout: 10000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

# Logging configuration (optional)
logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN

# Application-specific config (optional)
cinehub:
  api:
    version: v1

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
    resolve-schema-properties: true
  swagger-ui:
    url: /openapi.yml
    path: /swagger-ui.html


